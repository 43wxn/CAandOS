# Member B Demo Screenshots

These screenshots are for the member B report section.

| File | Suggested use |
| --- | --- |
| `00_boot_dterm.png` | System boots into `/bin/dterm`; A's boot/syscall baseline remains usable. |
| `01_meminfo_proc_home.png` | `/proc/meminfo`, `/proc` directory view, `/home` view, and `/home/welcome.txt` from `os-root/`. |
| `02_ramfs_rw_rm.png` | RAMFS file create/write/append/read/delete flow; `cat` after `rm` reports no such file. |

The command output screenshots were generated with a temporary auto-demo dterm build, then the normal dterm source and ramdisk were rebuilt afterward.
